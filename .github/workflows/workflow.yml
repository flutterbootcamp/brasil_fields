name: Dart CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    container:
      image:  google/dart:latest
    
    steps:
    - uses: actions/flutter@master
    - name: Install dependencies
      run: flutter pub get
    - name: Run tests
      run: flutter run test
